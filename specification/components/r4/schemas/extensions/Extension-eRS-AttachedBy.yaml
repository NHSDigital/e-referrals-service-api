title: Extension-Referral-State
description: Extension to supply the e-RS representation of the state of the referral.
type: object
required:
  - url
  - valueReference
properties:
  url:
    type: string
    enum:
      - 'https://fhir.nhs.uk/StructureDefinition/Extension-eRS-AttachedBy'
    example: 'https://fhir.nhs.uk/StructureDefinition/Extension-eRS-AttachedBy'
  valueReference:
    type: object
    required:
      - identifier
    properties:
      identifier:
        $ref: '../identifiers/UserId.yaml'
