description: An outcome of unsuccessful operation
content:
  application/fhir+json:
    schema:
      $ref: '../schemas/OperationErrorOutcome.yaml'
    example:
      $ref: '../examples/OperationErrorOutcome.json'