title: Details of code system properties
type: object
required:
   - code
   - uri
   - type
properties:
   code:
      type: string
      enum:
         - effectiveFrom
         - effectiveTo
         - appointmentBookingCancellationReasonType
         - willCancelWholeRequest
         - commentIsMandatory
         - specialty
   uri:
      type: string
   type:
      type: string
      enum:
         - code
         - Coding
         - string
         - integer
         - boolean
         - dateTime