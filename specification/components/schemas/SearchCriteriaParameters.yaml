title: The criteria used to search for services
type: object
required:
  - id
  - meta
  - resourceType
  - parameter
properties:
  id:
    type: string
    example: 'ServiceSearchCriteria-1'
  meta:
    type: object
    required:
      - profile
    properties:
      profile:
        type: array
        items:
          $ref: 'fhir/structures/eRS-ServiceSearchCriteria-Parameters-1.yaml'
  resourceType:
    type: string
    enum:
      - 'Parameters'
    example: 'Parameters'
  parameter:
    type: array
    items:
      anyOf:
        - $ref: 'parameters/Priority.yaml'
        - $ref: 'parameters/Specialty.yaml'
        - $ref: 'parameters/ClinicType.yaml'
        - $ref: 'parameters/AppointmentWaitTimeLimit.yaml'
        - $ref: 'parameters/Postcode.yaml'
        - $ref: 'parameters/DistanceLimit.yaml'
        - $ref: 'parameters/ClinicalTerm.yaml'
        - $ref: 'parameters/NamedClinician.yaml'
        - $ref: 'parameters/AgeAndGenderAppropriate.yaml'
        - $ref: 'parameters/CommissioningProvisioning.yaml'
        - $ref: 'parameters/Organisation.yaml'
