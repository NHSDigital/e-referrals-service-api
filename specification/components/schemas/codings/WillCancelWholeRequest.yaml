title: Will cancel whole request value
description: This will only be returned for appointment cancellation reason
type: object
required:
   - code
   - valueBoolean
properties:
   code:
      type: string
      enum:
         - willCancelWholeRequest
   valueBoolean:
      type: boolean
