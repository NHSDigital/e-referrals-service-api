title: Details the distance limit value specified in a search criteria
type: object
required:
  - name
  - valueUnsignedInt
properties:
  name:
    type: string
    enum:
      - distanceLimit
    example: 'distanceLimit'
  valueUnsignedInt:
    type: integer
    minimum: 0
    format: int32
    example: 123