title: location
description: The service location identifier
type: object
required:
  - name
  - valueIdentifier
properties:
  name:
    type: string
    enum:
      - location
    example: 'location'
  valueIdentifier:
    $ref: '../identifiers/STU3_Organisation.yaml'
