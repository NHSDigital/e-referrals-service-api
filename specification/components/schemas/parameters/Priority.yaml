title: Priority
description: Details the priority parameter
type: object
required:
  - name
  - valueCoding
properties:
  name:
    type: string
    enum:
      - priority
    example: 'priority'
  valueCoding:
    $ref: '../codings/Priority.yaml'
