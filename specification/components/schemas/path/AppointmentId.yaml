name: id
in: path
description: |
    The ID of the appoinment to be supplied. It must be valid and relate to an existing Appointment Id otherwise a HTTP 404 response will be returned.
required: true
schema:
  type: string
  example: '11000'
  