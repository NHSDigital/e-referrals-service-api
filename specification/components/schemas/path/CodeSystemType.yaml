name: codeSystemType
in: path
description: |
   The requested code system
required: true
schema:
   type: string
   enum:
      - 'SPECIALTY'
      - 'APPOINTMENT-CANCELLATION-REASON'
      - 'CLINIC-TYPE'
      - 'REQUEST-CANCELLATION-REASON'
   example: 'SPECIALTY'