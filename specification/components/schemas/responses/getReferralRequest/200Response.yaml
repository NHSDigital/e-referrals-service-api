description: e-RS Referral Request
headers:
  X-Correlation-Id:
    $ref: '../../headers/response/CorrelationID.yaml'
content:
  application/fhir+json:
    schema:
      $ref: '../../ReferralRequest.yaml'
    examples:
      unbooked:
        summary: Unbooked Referral Request
        description: A minimal response
        value:
          $ref: '../../../examples/getReferralRequest/responses/Unbooked.json'
