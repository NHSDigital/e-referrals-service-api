title: referralRequestVersion
type: object
required:
  - url
  - valueInteger
properties:
  url:
    type: string
    enum:
      - referralRequestVersion
    example: referralRequestVersion
  valueInteger:
    type: integer
    example: 0
